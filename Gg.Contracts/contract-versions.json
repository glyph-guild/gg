[
  {
    "version": "0.1.0-alpha.24",
    "surface": "a87f0af7d73c74e00da7811a7515005206c372c565b41284f4a9ff00571ad95d"
  },
  {
    "version": "0.2.0",
    "surface": "a87f0af7d73c74e00da7811a7515005206c372c565b41284f4a9ff00571ad95d"
  },
  {
    "version": "0.3.0",
    "surface": "a87f0af7d73c74e00da7811a7515005206c372c565b41284f4a9ff00571ad95d"
  },
  {
    "version": "0.4.0",
    "surface": "7f15b2db0ac4620ccd3087efb0daa5fb74444186be7687729917a975fe318cc2"
  },
  {
    "version": "0.5.0",
    "surface": "8f7be9fa57192b3ed395858339fd24d72c1fb7613b6bb94de901157ac23a795f"
  },
  {
    "version": "0.6.0",
    "surface": "0d2bdd5a0676583a7df6db8a413a8d68c36901e565d5dd1ed18f2da034ddb466"
  },
  {
    "version": "0.7.0",
    "surface": "6de623bea8f160a3e0b03f2e5eeb3aceb0c712529bc2585a9a69e1910ec66534"
  },
  {
    "version": "0.8.0",
    "surface": "38b65bfdd7bca12827df108d9dbfb8139c642e3e6835017b708dbdf3d61f6d81"
  },
  {
    "version": "0.9.0",
    "surface": "0a18b329476e84f3b7ba35f0a1c741ce5358b28966842218d57c34e2980422eb"
  },
  {
    "version": "0.10.0",
    "surface": "56e6575a2a82dc3672652c973995410e055a1232038152b95756e059997de707"
  },
  {
    "version": "0.11.0",
    "surface": "082f58779b59aed891414e5127a3845a75f989bb0897edde084964decab083c4"
  },
  {
    "version": "0.12.0",
    "surface": "83aaa9d2fc641da0957d590b167cd4c702138fc7c268dad88305e052aa43b7a7"
  },
  {
    "version": "0.13.0",
    "surface": "83aaa9d2fc641da0957d590b167cd4c702138fc7c268dad88305e052aa43b7a7"
  },
  {
    "version": "0.14.0",
    "surface": "bfae16184fa0ccf378a487e8e66f7434b59bccd973fa8c3a19cc7bf09416837c"
  },
  {
    "version": "0.15.0",
    "surface": "5244d0af4a9f748487d528a82cad74e63f19b74129c683d55ad90e5850e98eec"
  },
  {
    "version": "0.16.0",
    "surface": "45069142a0ebf8c5c783818f28a1d92f7b9198155a8c93fc37d8f3509112f10c"
  },
  {
    "version": "0.17.0",
    "surface": "ac938733e0f0bbb5cfe2654c1713254c98ce09ba3e644503c0c1f6133c9ad8be"
  },
  {
    "version": "0.18.0",
    "surface": "5accb9d5070e92ac4b0019afd0c1a6046a60c80adb57753936302682d4c8603f"
  },
  {
    "version": "0.19.0",
    "surface": "5accb9d5070e92ac4b0019afd0c1a6046a60c80adb57753936302682d4c8603f"
  },
  {
    "version": "0.20.0",
    "surface": "4e82ee89cbec3648688c9229bbf2b463be79c0de8c3ad25c8557eafea72d1524"
  },
  {
    "version": "0.21.0",
    "surface": "dfbeb578a56792a0798cf6bbceead3f38a443ba542e76b26388b9f1c3bce4ef8"
  },
  {
    "version": "0.22.0",
    "surface": "dfbeb578a56792a0798cf6bbceead3f38a443ba542e76b26388b9f1c3bce4ef8"
  },
  {
    "version": "0.23.0",
    "surface": "dfbeb578a56792a0798cf6bbceead3f38a443ba542e76b26388b9f1c3bce4ef8"
  },
  {
    "version": "0.24.0",
    "surface": "6aa6a83cad02ccb025eee8f929bfca22a12adbb033a711755eec0f04e12ab164"
  },
  {
    "version": "0.25.0",
    "surface": "5ebbd99eaabc54583e8213e6eb37bafefdebc43425c0f5dbfc1e9794c4e7097e",
    "note": "check: human and its approver; the push and the proposal as two separate permissions (BranchPush beside DestinationAdmission); destination.pushed as a ninth fact kind carrying the commit a pending decision is about, with destination.landed untouched so nothing recorded is reinterpreted; GET /v1/gates and the gate list. Fact vocabulary moves to 0.10.0 with it."
  },
  {
    "version": "0.26.0",
    "surface": "f28c265770a357f942b88607f99c2985effedf4a15d4ebd7b29784bac32ead8d",
    "note": "The answer path: POST /v1/flights/{ref}/decisions, DecisionRequest carrying the evidence manifest hash it was made against, DecisionRecorded carrying the re-evaluated admission, and DecisionObservations - what gg observed about how a decision was made, interpreted by nothing. PendingGate gains the manifest hash a decision is scoped to and the attempt count. All new types and new members; no enumeration that already crosses gained a value, so the fact vocabulary stays at 0.10.0."
  },
  {
    "version": "0.27.0",
    "surface": "21a2eb1f036664706763bf8777d62b69cb9c6d7cee8586af1dd866171fa90741",
    "note": "Reject: DecisionOutcomes gains `rejected`, DecisionRequest gains the person's reason, and a lease carries LeaseFeedback - the sentence that sent an attempt back, attributed, marked as their words, and carrying nothing that could widen a move, a scope, an obligation, a destination or a budget. DiffBasis gains `prior-attempt` for a manifest measured from the previous attempt's head. And the fingerprints now hash closed vocabularies' VALUES: `rejected` and `prior-attempt` are both added values, and until this version neither ledger could see one. Fact vocabulary moves to 0.11.0 with it."
  },
  {
    "version": "0.28.0",
    "surface": "165850ebee034f245d5df4c651f367313605a7863458be3e28ac383699b346b7",
    "note": "The second attempt's push: a lease repository reference gains `continuesFrom`, the commit a previous attempt pushed, so the runner is TOLD where to continue rather than inferring it from a branch anybody with push access can move - and so a manifest's prior-attempt basis and the commit it measures from cannot disagree. A flight summary gains `attempts`, counted from loops that ran rather than leases that were granted. Both are added members; no member moved and no value was added to a closed vocabulary."
  },
  {
    "version": "0.29.0",
    "surface": "b03b9bc2db32ad6b2657be8ab961e80d0743c39b469a4a89aa79e19b3ac7bba6",
    "note": "The evidence payload. Obligation gains `evidence` - what a gate needs before anybody can answer it, declared in the envelope so the requirement is reviewed configuration rather than whatever the assembler had to hand, and an entry the flight cannot produce halts it rather than rendering a blank. Three new pinned types carry the case put to a person: GateEvidencePayload, GateEvidenceItem and EvidenceReference, the last with deliberately nowhere for a body to travel. Three new closed vocabularies with them: EvidenceItems, EvidenceDispositions - inline, digest, reference, and no fourth, so a shortened item is unrepresentable rather than merely unused - and EvidenceVoices, which marks a piece of evidence as measured or stated so an agent's words stay visibly different in kind from a verdict computed from facts. Also carries the vocabulary-membership attribute and its closure check; see fact-vocabulary 0.12.0 for why."
  },
  {
    "version": "0.30.0",
    "surface": "b6c7ae1cb9c92d5f4e9eb5358f87bd3e0a9c94f172500e61ca032614ff47b8c0",
    "note": "The write move and the environment's account of what bounds it. LoopMoves gains `write` - a VALUE in a closed enumeration, so it costs a fact-vocabulary bump to 0.13.0 and its ledger entry, and it is what finally lets a flight create a file. environment.identity gains moveEnforcement and movesProbed, behind a new MoveEnforcements vocabulary, so a fact set says how much this machine's executor actually bound and what was proven rather than assumed. LoopBudget gains attempts - flight-level, the reject-and-rerun cycle, and NOT the agent's internal turns, which are already called attempts on loop.outcome and would spend a budget of five on one loop thinking. A member, so it costs nothing and rides the value's bump."
  },
  {
    "version": "0.31.0",
    "surface": "ac663b8e4af0e5aad40e6e64a37c8ae350e5c8435e87bfb44fb09510a9ce834e",
    "note": "Layers. ObligationProvenances gains `flight` and is declared ORDERED - it is a ranking, and ClosedVocabularies sorts a vocabulary's values unless told otherwise, so without this swapping org and flight would change which layer outranks which and move no fingerprint. That defect was found once already on the egress levels. No `team`: the design has three layers, this ships two, and a value nothing can produce is a value nobody maintains - its arrival is a version move and its absence is asserted. EnvelopeLayer and Composition carry a document with the layer BESIDE it rather than inside it, so a flight-level file cannot claim org. Composition is add-only rather than narrow-only: general widening detection over predicates is undecidable, and a lower layer adding its own while never touching a higher layer's has the same effect and a yes-or-no answer. Strengthening is adding, so there is no edit-a-higher-layer operation to secure. The parser now REFUSES a declared provenance and EnvelopeText.Render no longer emits one - what an author writes has no layer, and RenderComposed annotates the flattened form as a report that deliberately does not round-trip. A `when:` citing a verdict is refused by name, saying what breaks and naming the open question."
  },
  {
    "version": "0.32.0",
    "surface": "4369deaca2410dfe0738394a47f687d14474ac5668b9c8a889114d5a8536991a",
    "note": "The envelope change as a FLIGHT. Two values in closed vocabularies, so both cost a version and every prior reader halts on them rather than guessing. ExecutorRungs gains `human` - a loop nothing automated runs - named rather than borrowing `frontier` with nobody listening, because a rung says what discharges the loop and recording an agent rung for work no agent does makes every later count of how much the machine did wrong in the flattering direction. It is a value in a Fact-fingerprinted vocabulary, so it also bumps fact-vocabulary to 0.14.0 with NO kind added; see that entry for why an envelope change produces no fact. DestinationKinds gains `envelope-change`, the first destination that is not somewhere code goes - which is what makes 'phase 2 arrives by adding a destination' a claim that can be tested rather than restated. And a new refusal: a loop at the human rung may declare no moves, because moves are bound by the executor a runner starts and this rung starts none, so a declared move would be granted by the envelope and enforced by nothing. That is the shape `write` was added to stop being, arriving through a different door. No member moved and no type was added."
  },
  {
    "version": "0.33.0",
    "surface": "72a7a4afcce0349b8eaaae927360b5a3941f8e64c2035a1e08e38cfa54e07694",
    "note": "A gate about something that is not a repository. PendingGate.Branch and .Commit stop being required and become nullable. This is the coupling that flying a flight with no repository actually found: every gate this contract could describe carried a branch and a commit, which was true of every gate that had ever existed - and made `flight` mean `agent run against a branch` in the one type a person reads when they are asked to answer for something. Null and never an empty string, because a gate carrying commit:\"\" makes 'no commit' and 'a commit nobody recorded' the same value, which is Article XI's failure with the fields swapped and the harder one to notice - the renderer prints a blank either way. The rule those members encoded is unchanged: a gate about work in a repository still waits for the push, because a decision about a tree on somebody's machine is a decision nobody can act on. That is now a rule about repository DESTINATIONS rather than a rule about gates. Two members loosened; nothing added, nothing removed, and no value added to a closed vocabulary - so the fact vocabulary does not move."
  },
  {
    "version": "0.34.0",
    "surface": "c67a06f3b4448e8ea9f8fb63f72211d18fb57f58a710e63071270c840a857921",
    "note": "The verdict, closed on the wire. ObligationAttribution.Outcome was an unconstrained string while Attachments - in the same file, read off the same endpoint by the same client - was closed and fingerprinted. Three copies of `satisfied`/`violated` existed and none of them was here: the control plane's event vocabulary, a private pair inside the Engine whose own comment explains that two spellings of `violated` is a comparison failing silently in the permissive direction, and nothing at all on the boundary. So a value added to the verdict moved no ledger and asked nobody to think about it, which is the defect the vocabulary mechanism exists to close, sitting inside the mechanism's own file. It carries `unevaluable`, which nothing writes - a halted flight records no verdict at all, because a verdict set with a hole in it reads as a complete answer - and it is declared because the store can hold it and a wire vocabulary missing a value the writer can emit is the permissive silence one spelling away. It shares that word with Attachments and means something else by it: there the CONDITION could not be read, here the obligation applied and could not be MEASURED, and merging them would make 'this rule did not apply' and 'this rule could not be checked' one value. Closed now rather than earlier because good-grief:DecisionVisibilityTests first had to prove the value arrives on this surface - a spelling nobody checks against a behaviour nobody measures is not a guard. No member moved, no type was added, and the fact vocabulary does not move: a verdict is produced by the control plane, not by a runner."
  },
  {
    "version": "0.35.0",
    "surface": "c67a06f3b4448e8ea9f8fb63f72211d18fb57f58a710e63071270c840a857921",
    "note": "ADR-0012 B2: the decision endpoint stops answering inline. POST /v1/flights/{ref}/decisions declares 202 with no body instead of 200 with a DecisionRecorded, because the write becomes a command and answering inline would mean the endpoint waiting for its own event to land - the blocking read the whole change removes. 409 survives and that is not an inconsistency: a decision made against work that has since moved is refused BEFORE anything is dispatched, from state the request already has, so what became asynchronous is the recording rather than the check on the way in. The client tolerates BOTH answers for now, which is what lets the two repositories land this in either order; when no control plane answers inline the 200 branch is dead and deleting it is a change with its own reason. THE SURFACE HASH IS UNCHANGED AND THAT IS A GAP, not an oversight in this entry: ContractSurfaceTests hashes pinned types, their members and closed vocabularies, and reaches ProtocolSurface.Endpoints not at all - so a route's path, method, audience, statuses and response type can all move without the version that describes the contract moving. This is the closed-vocabulary defect one layer up, and the same sentence applies: the guard that exists to force this conversation could not see the change that most needs one. The version is bumped by hand here BECAUSE the mechanism did not ask, and EndpointSurfaceTests now pins a fingerprint of the routes so the next one is at least loud. Folding that into the contract surface proper would move every recorded hash and belongs in a step about the mechanism."
  },
  {
    "version": "0.36.0",
    "surface": "e0b1354b41e16ecec3ff9fc3386cd93c3cc22c12f3b18f157c8f384a6f32e36b",
    "note": "Shipping facts is accepted rather than answered, and the landing decision moves to a route of its own. The control plane's evidence write becomes a command (ADR-0012), so POST /v1/leases/{id}/facts answers 202 and its body reports only what was refused before anything was written - validation, cleanliness, classification and budget are all decided from the request itself. Accepted and duplicates leave: both are answers only the write has, and duplicates especially, because dedupe moves into the projection where a repeated event is ignored rather than counted. Push and admission could not simply be dropped. LandAsync reads both and treats each as REFUSED when absent, which is correct while the answer arrives with the write and a silent bug the moment it does not: 'not evaluated yet' and 'no' become the same value, and a flight that was going to be admitted quietly never pushes. So they move to GET /v1/leases/{id}/admission, and LandingDecision carries `settled` - absence means refusal only once the question has been answered at all."
  },
  {
    "version": "0.37.0",
    "surface": "31e6b91862aedb292dc3942f06f2f6e393a40c3785c10378709256c90ee5e6b0",
    "note": "An invitation into an existing tenant. POST /v1/invitations answers with a URL and an expiry, and /v1/invitations joins the governed prefixes so the declaration closes over it - an invitation is the strongest capability the product issues, because whoever holds the link becomes a principal in somebody else's tenant, and an undeclared route under that prefix would be an unaudited way to make one. InvitationRequest is EMPTY and stays empty: an invitation names nobody. Carrying an address would imply the control plane delivers it, which it does not - the person who ran the verb does that, by whatever means they already trust - and carrying a tenant would let a caller name one rather than be one. InvitationIssued echoes a URL the control plane built, exactly as DeviceAuthorizationStarted echoes a verification URI: where the web surface lives is deployment knowledge, and a client that composed it from a base address would guess wrong the first time somebody deployed it anywhere but a laptop."
  },
  {
    "version": "0.38.0",
    "surface": "b2e312cc885ba5ed6882c209a1c0be938426e714791dc8a507ac7469a8dd0162",
    "note": "The claim becomes a request with a status. POST /v1/leases:claim answers 202 with a LeaseClaimAccepted - a request id and an interval - and GET /v1/leases/claims/{id} says what became of it. Both old answers go, and losing the 204 is the point rather than a side effect: an idle fleet and a fleet blocked on a credential nobody has registered were the SAME answer, so the only thing a runner could report was silence, identical to the silence of a system with nothing to do. That was tolerable while a claim could be decided from state the control plane already held; it stopped being tolerable when identity moved behind an announcement and 'the credential is not here yet' became a real state with nowhere to live. LeaseClaimStates is closed - pending, waiting, granted, expired - so a fifth costs a version and every prior reader halts rather than guessing, and `waiting` is the one that did not exist before. LeaseClaimStatus.Lease is absent unless the state is granted, which is LandingDecision.Settled's arrangement for LandingDecision.Settled's reason: absence has to mean one thing. pollAfterSeconds is server-supplied and load-bearing rather than a hint - the control plane holding the claim open WAS the rate limiter and the runner has no backoff of its own, so a cadence the runner invented would either hammer an endpoint that now answers instantly or idle past work that was ready; DeviceAuthorizationStarted.PollIntervalSeconds is the same arrangement for the same reason. LeaseGranted gains unresolvedRepos, which fixes a silent failure older than this change: an empty `credentials` was two facts - no credential registered, or one not yet known to this control plane - and the runner's TryGetValue result was never consulted, so a repository with neither fetched ANONYMOUSLY. A public one succeeded and a private one failed later at git's own words with nothing pointing at the cause. Naming them turns that into a refusal before anything is materialized, which is what the push path has always done. The consequence is deliberate: a public repository with no registered credential is now refused rather than cloned anonymously, because absence cannot keep meaning both 'needs none' and 'nobody registered one' - that ambiguity IS the bug. A member on LeaseGranted, three new types, and one new closed vocabulary; the fact vocabulary does not move, because none of this is something a runner reports."
  },
  {
    "version": "0.39.0",
    "surface": "6a7fa7be43dc6cd01501fc04be822c399d8774f77f3a34a42ddd1053e7207b03",
    "note": "The seed becomes a wire resource, served at GET /v1/flights/{ref}/seed. TakeSeed, TakeMeasurements and TakeSeedComposer move out of Gg.Client and into this package, because the control plane composes the seed now and the two repositories cannot reference each other - only this package crosses. The composer moves WITH the types deliberately: reimplementing the composition on the other side would be two derivations of one document and they would drift on the first change to either, which is the same argument CredentialLocator.ForRepo is here for. TakeSeed.TreePath is GONE, and its absence is the point of the change. It said 'where the work is, on this machine', and that one member is why a stopped flight was resumable by whoever sat at that keyboard and by nobody else. Git already moves a working tree between machines; what git cannot reconstruct is what was tried and ruled out, and that is what a seed is. LoopDigest.Validate already refused an absolute path for exactly this reason - 'a digest carries paths a person can compare across flights, not paths on one machine' - so the fact was designed portable while the feature was built local. In its place: a transcript reference and a state that SAYS where it is. A transcript holds the customer's own code and never crosses, and ArtifactScopes has exactly one value, so what a person on the far side of a handoff gets is a hash and a locator they cannot follow. TranscriptStates is closed with two values - elsewhere, none - and there is no `present` because the platform cannot produce one; declaring a state nothing reaches would be a promise it does not keep. The two read differently on purpose: an absent section and an artifact that does not exist send a person to different places, which is the same rule the account's NO ACCOUNT line already follows. AccountState stops being a C# enum and becomes AccountStates, a closed string vocabulary. An enum crossing this boundary serializes as an INTEGER: unreadable to the auditor this package exists for, and invisible to the mechanism that discovers closed vocabularies by shape and forces a version when one gains a value. It was only ever an enum because the seed was a document one console composed for itself. And the seed carries a revision of its own. A second number beside the protocol revision, which earns itself: the protocol revision says which conversation the two sides are having, and this says what shape a document an AGENT reads as declared context is in. A shape that changed silently would change what every future resumption knows with nothing anywhere to point at. Two pinned types, two closed vocabularies, one route. The fact vocabulary does not move: a seed is composed FROM facts and is not one, and nothing a runner reports changed shape."
  },
  {
    "version": "0.40.0",
    "surface": "10f1b5106c1685f63f3c5677bec9dc653464b52a65232b841c39223e1a246406",
    "note": "A takeover becomes a HOLD, and this is the first entry in this ledger that REMOVES a route. POST /v1/flights/{id}/takeover is gone and three replace it: takeover:claim, takeover:renew, takeover:return. What went wrong with the old one is when it was written. It took a TakeoverRecord carrying heldForMs - how long somebody held the flight - so it was posted once they had already finished. That is a record, not a hold: two people on two machines both see a takeable flight, both take it, and both find out afterwards. Nothing about it was atomic, because it was never asking for anything. Correctness here needs a claim, which is the same answer the lease and the device code already have - and the direction this fails in is the dangerous one. A read model lagging over a permission table fails EMPTY, which is a liveness problem; lagging over a HOLD fails permissive, and the second claimant takes a flight somebody is working. Deleted rather than deprecated beside the claim. It was reachable only through a client method nothing in the product called, because nothing in the product ever took a flight over - unreleased in effect - and a route that records a takeover without holding anything, sitting next to the claim that replaces it, is a shape somebody will build against. TakeoverRecord goes with it, for the same reason: a wire type no route carries is a type somebody will find and use. The claim answers 409 with TakeoverHeld, and that refusal is the product rather than an error path. 'Somebody else has this' sends a person nowhere; a name and two instants tell them whether to wait, to ask, or to come back. It is also the only thing standing between a portable handoff and two people confidently editing divergent copies of one flight, each believing they hold it. The renewal exists because a hold has to expire on a CLOCK. Attendance lapses - somebody closes their laptop - and judging whether a person is still at their desk is exactly what ADR-0005 refuses. So the hold has a window, the holder renews it while they are working, and renewWithinSeconds is the server's number rather than the client's: a cadence a client invents is wrong on one of the two machines this feature exists to span, and DeviceAuthorizationStarted.PollIntervalSeconds is the precedent. The generation is the fence, exactly as it is on a lease: a holder whose hold lapsed and whose flight was then claimed by somebody else is told so rather than handed it back. There are TWO return types now and the split is about who writes each. TakeoverReturn stays as it is: a file a person writes by hand, naming the flight, because a file left over from a previous takeover parses perfectly and describes a different flight and applying it would put one flight's decision onto another. It cannot carry a generation because nobody types one. TakeoverReturnRequest is what gg posts - the flight is in the path, and the generation is what stops a decision landing on a hold that has since moved. What it deliberately does NOT carry is how long the flight was held: the control plane knows when the hold was claimed and what time it is, and a duration supplied by the party it is about is not a measurement. Five pinned types added, one removed, one route removed and three added. The fact vocabulary does not move: a hold is not something a runner reports, and no fact changed shape."
  },
  {
    "version": "0.41.0",
    "surface": "9d1bb737144c6c11955f401d96dc9dd383d488997438f35e8ed8cb46e5bca1ac",
    "note": "The envelope decides whether work that was NOT admitted may still reach the forge. Destination gains preserve-unadmitted, DestinationPushed gains preserved, and DestinationBranch gains ForHandoff. Why it is needed: a flight that halted, violated or exhausted has no branch anywhere. HandoffRoot's own comment names it - 'a landed flight has a branch and a proposal, a violated or exhausted one has neither, and the work exists only here, which is precisely the flight somebody wants to take over'. So the one flight most likely to need taking over is the one that cannot be taken over from another machine, because its work is on a disk. Why it is a permission rather than a behaviour: inverting that is not free. Unadmitted work would exist on the customer's remote, and a violated flight's code reaching the forge is a governance question rather than a plumbing one. AdmissionEngine.Preserve already refuses to ASK A PERSON about work that failed a machine check, and the same argument does not automatically extend to PRESERVING it - so the envelope says, rather than the platform deciding. ABSENCE MEANS NO, and that is the load-bearing half. The failure mode is a tenant discovering that every abandoned agent attempt is a branch on their default remote, so an envelope that does not name this pushes nothing - which is also exactly what every envelope written before 0.41.0 continues to mean. Article XI, and the same reason an unknown predicate halts rather than evaluating false. Null and false are one answer and there is no third state a reader could take for yes. A MEMBER RATHER THAN A VALUE, so no reader has to halt: a member may be added freely and a value in a closed enumeration may not. That is why this costs a contract version and no vocabulary. It is refused on a destination that is not a pull-request. An envelope-change destination has no branch and no repository, so the permission names nothing there - and a knob that silently does nothing on one kind of destination is one somebody sets and believes they granted. The text form writes it only when it is declared. Emitting preserve-unadmitted: false for every destination that omits it would rewrite every tenant's document on the next `envelope show`, and a diff nobody made is how a review practice gets abandoned. The parser reads only 'true' and 'false' - YAML dialects accept yes, on and y as truth, and a governance permission whose meaning depends on which parser opened the file is not a permission. ForHandoff is a second branch name because it is a second fact: gg/handoff/GG-42 rather than gg/GG-42, so a flight preserved for handoff and the same flight later admitted cannot fight over one ref - the second push would either be refused as an existing branch or would overwrite the work somebody was about to take over. It stays under the gg/ prefix so whatever cleans up this platform's branches still recognises it. DestinationPushed.preserved moves the FACT vocabulary as well, to 0.15.0: a gg/ branch with no pull request is not a proposal, and a reader counting branches has to be able to tell work that was admitted from work that was merely kept."
  },
  {
    "version": "0.42.0",
    "surface": "559205911e19d3428c77fed5ff3011cc97c01c0f0143afa845c1212012167979",
    "note": "An exhausted loop can hand its work to another loop. ExhaustionPolicies gains handoff-to-agent and LeaseLoop gains resumesFrom. A VALUE in a closed enumeration, which is a break rather than an addition: the only safe response to an unknown value is to halt, so every prior reader meeting handoff-to-agent stops rather than guessing what to do when a budget runs out. That is the behaviour and not a defect. Existing envelopes are unchanged in meaning, because none of them named it - handoff-to-human still parses, still validates, still renders identically, and still means a person is asked. It needs no new machinery, because `executor` is already a parameter of a loop: an agent resuming another agent's work is the same loop starting again on a rung, with the seed as declared context. What was missing was somewhere for on-exhaustion to point. It is bounded by LoopBudget.Attempts and by nothing else. That field already counts how many times a flight's loop may run; without it this value would be an instruction to retry for ever, which is a termination condition nobody agreed to. LeaseLoop.resumesFrom is the load-bearing half and the reason it is a member on the LEASE rather than a route: GET /v1/flights/{ref}/seed answers a DEVELOPER session, deliberately, because a runner able to call it could read what every flight in the tenant tried and ruled out from a credential meant only to let it hold one lease. So a resuming loop is handed its context on the lease it already has, which is also the shape ContextBinding has. It carries the RENDERED seed rather than the model: it reaches an executor as declared context beside the intent, and rendering it runner-side would be a second implementation of a document this package already renders once. Absent on a first attempt, which is the ordinary case - there is nothing to resume from. A member that had to be present would make every lease carry an empty document, so 'no prior attempt' and 'a prior attempt that measured nothing' would read the same. Nothing in it is customer content: it is composed from facts that already crossed, and TakeSeed carries no absolute path and no machine name by construction - the transcript is named and never included. The fact vocabulary does not move. An on-exhaustion policy is read off an envelope and a lease is not a fact, so neither is something a runner reports."
  },
  {
    "version": "0.43.0",
    "surface": "15fa446aba4e38bcedfba30ae2af45986eb2a27903b52a1651b6afdf6ebb02f7",
    "note": "The envelope learns to ask for an environment, and the fleet learns to answer. Two SELECTIONS arrive on the envelope - environment and repository - as optional members, which is the free kind of change: every prior document parses, validates, renders identically, and means what it meant. A selection is deliberately not a bound: every other envelope field composes across layers through a merge operator, these get none, because two layers naming different environments is not an empty intersection, it is a mistake, and the composer refuses it rather than merging it. Declared once, validated for MEMBERSHIP at apply against the tenant's chart, never merged. The contract owns only the shape (one line, non-blank, absent-stays-absent); membership is the control plane's question because the control plane has the chart. Re-applying an existing envelope under this version mints a new envelope version - the ADR-0013 hazard - and that is a decision, not an accident: the fields land in today's flat envelope now and migrate into role-shaped types when the named-envelope ADR lands, two minting events budgeted with eyes open. The chart itself crosses the wire as ChartEnvironmentRequest / EnvironmentCharted / EnvironmentChart under a NEW governed prefix /v1/environments: the chart decides what an envelope may say and what a fleet must satisfy, so an undeclared route under it would be an unaudited way to widen every envelope - the same argument /v1/credentials came in on. A chart entry's meaning is optional and its absence IS the disposition: LabelDispositions (measured | stated) arrives as its OWN closed vocabulary rather than reusing EvidenceVoices, though the words coincide today, because one says what a gate's evidence is and the other what a capability claim is, and coupling two closed enumerations makes a value added for either a halt for readers of the other. AdvertisedLabel carries the word beside the name and RunnerSummary gains labels, so a stated claim can never be read as a measurement by losing its qualifier in transit. The checklist - Checklist and ChecklistItem, served at GET /v1/envelope/plan (tenant-level: what WOULD a flight need) and GET /v1/flights/{ref}/checklist (pinned to what that flight compiled) - is the third flight artifact: the plan says what would happen, the log says what happened, the checklist says what must hold first. Its satisfier vocabulary is CLOSED at two values, already-true-via-matching and nobody-declared-capability-gap, and the closure is the design: a third value means a strategy exists, which is a design event that arrives as a deliberate contract change, not a string. FlightSummary gains requiredLabels and waiting - refusal at apply, waiting at flight: apply has an actor at the keyboard to inform, a queued flight has nobody, so it waits loudly with the sentence on the one document gg flights, gg show and --json all read. Null means not waiting, the LeaseClaimStatus.Lease absence rule. All members and new types; the only closed vocabularies added are new ones, so nothing any prior reader knew changed meaning."
  },
  {
    "version": "0.44.0",
    "surface": "a5abfed41e7089aea45a6c0f8889e4f7feb95d0066868d6e35bfdcb3eb588eae",
    "note": "Role-shaped types, and the round trip stops lying. EnvelopeNarrowing arrives as an obligations-only document that syntactically cannot carry a loop set, a destination set or a context selection - the strongest form of the operator table is one a document cannot express, so there is no Validate refusal for those to get wrong. It is pinned and fingerprinted but no route serves it yet: narrowings-by-name are slice nine's pre-committed cut, and until they land the type's liveness is the round-trip suite, which is stated here so the audit trail cannot read it as served. The emitter and parser fork by role - Render/Parse for the full envelope, Render(EnvelopeNarrowing)/ParseNarrowing for the narrowing, the role chosen by which door was knocked on and never by a key in the document, the same rule as layer: and provenance:. Both paths now carry model-preservation round trips (parse(render(x)) == x, member by member), because the previous render-idempotence test is exactly how the evidence: defect survived: EnvelopeYaml has parsed evidence: since it existed and EnvelopeText never emitted it, so gg envelope show -> edit -> apply silently removed a gate's evidence requirement and the weakening minted an attributed version with nothing marking the removal - fired live at slice nine step 0 before this was designed against. evidence: now renders last in the obligation block and attempts: after wall-clock (the same defect through the other door: stored via the wire, invisible in show, refused by the parser on the way back in), both written only when declared, so every document that never declared them keeps its exact bytes. A member-accounting ratchet in the round-trip suite now forces the decision the two dropped members never got: a new text-expressible member fails the build until its text form is decided or its exemption is written down. This is the second minting event slice eight budgeted with eyes open - and it lands after the control plane's dedupe moved from the stored bytes to the model, so re-applying unchanged policy across this bump mints nothing; Envelope itself is unchanged member-for-member, so its canonical JSON does not move either."
  },
  {
    "version": "0.45.0",
    "surface": "fcdc6c18ecaa9301afe0b0630bff6675f48d9e8b2047a5ce4a7a4dde737699c1",
    "note": "The topology reaches the wire, and unreachable gets a door. Slice nine's per-name envelope streams landed control-plane-side with a topology registry deciding which names exist at all - a name with no entry is unreachable, not merely unused, and the apply refusal says so by name. This version is that machinery's wire surface: DeclareNameRequest / TopologyName / EnvelopeTopology under a NEW governed prefix /v1/airspace, POST /v1/airspace/names as the door the refusal points at (v0 unrestricted and attributed, the chart's shape; who MAY declare stays with ADR-0016's closure set), and GET /v1/airspace/topology as what gg airspace show renders. root is never declared and never refused-by-absence: it is synthesized by every read, so the floor is in the answer before anything else exists and there is no row a bookkeeping edit could repoint. Role and parent ride as plain strings for now - the role vocabulary becomes contract data when composition's operator table lands, which is the next minting step's business, not this one's. All member additions and new types; nothing any prior reader knew changed meaning, and the envelope itself is untouched."
  },
  {
    "version": "0.46.0",
    "surface": "f0829dbe2e44dda9ce12bdbe1f29e95d1a81a2baec8f20991f8b78bd0fa054ea",
    "note": "The ranking dies, and the operator table becomes data. ObligationProvenances - the ordered [org, flight] vocabulary that was both the list of layers and the ranking - is deleted, and ADR-0014 records the ranking's reasoning as wrong: it was an artifact of ordered[0]'s replacement semantics, which silently took five fields (context, environment, repository, loops, destinations) from the highest layer and discarded every lower document's. What replaces it: Roles (root | work-kind | narrowing), deliberately NOT ordered, because position-as-authority is exactly what was deleted; MergeOperators (root-only | work-kind-only | intersect | min | union) declared per schema field as [Composes] data, with the composer building its table by reflection and THROWING on a field that neither declares nor carries a written exemption - the fact-vocabulary drift guard, applied to composition. preserve-unadmitted is the one deliberate exemption: ADR-0014's open question on its safe direction, kept visibly open rather than closed by a default, and unmovable meanwhile because a narrowing cannot express a destination at all. Obligation.Provenance is retyped from the layer string to ObligationProvenance { role, name }, so 'why did this gate appear' answers with a word a person recognises; EnvelopeLayer is reshaped to carry (role, name, parent) beside a full document or a narrowing, and Compose verifies the chain it was handed - one root named root under nothing, every parent present, one work kind, two documents claiming one name refused. A document declaring a field its role may not MOVE is refused naming the layer, the field and the operator; an echo of the governing value is not a move, because work kinds are full envelopes and Validate requires their members. Scopes intersect by containment, and two scopes where neither contains the other are refused naming both rather than resolved by a glob nobody wrote. Not yet wired: root's grants: block (the bound the work-kind sets intersect against - it lands with work kinds at creation, which is the next step's business), and the min/intersect operators on loop internals fire when narrowings grow those members. The provenance reshape changes the stored JSON's member shape; the control plane reads legacy string provenance tolerantly at its own door and its dedupe compares models, so no version mints for this bump."
  },
  {
    "version": "0.47.0",
    "surface": "4689ac2616a98fa62de2d9240aa765601af9ea391aa73972f60a6d564b96ad67",
    "note": "Flight creation declares what the flight is FOR, and where it will run. Three optional members on FlightLaunchRequest - workKind, environment, repository - the free kind of change: every prior client launches exactly the flight it launched yesterday, because absent means what it always meant. workKind names a work-kind document in the tenant's topology; null is implement, the kind every flight before kinds existed was. The two shipped kinds - implement and amend-envelope - are reserved names synthesized by every topology read, the root treatment: never a row, never declarable, and a tenant MAY apply an envelope to either, which then composes beneath root by the operator table. What a flight is for is knowable at the start - that is the test that reconciled declaring a kind with the old classification rejection; what the work turns out to touch stays a narrowing attached from facts, and a fact-attached narrowing may never grant. environment and repository move the SELECTION to the flight, validated by the control plane against the composed envelope's bound (exact match while the bound is single-valued; a granted SET is root's grants: block, which arrives when the sets' intersect-against-grants operator is wired, deliberately not now). With that, slice eight's org-layer-only selection refusal retires into the machinery that replaced it: the narrowing type cannot express a selection and the root-only operator refuses a work kind moving one, so the bespoke check dies with its job done. gg fly declares none of these yet and rides the defaults; the flags are a client feature, not a protocol one, and arrive on their own merits."
  },
  {
    "version": "0.48.0",
    "surface": "9fd7ea578e8660487eb7d724f024c70f9bc23936533f23ce67c09434655e0487",
    "note": "The repository registry, and the host leaves the request path. RegisterRepositoryRequest / RepositoryRegistered / RegisteredRepositories under the already-governed /v1/airspace prefix: a name, a provider KEY, the forge's immutable id, a display path - and neither a credential nor a host, by design, because which host a runner sends a customer's credential to is a runner-side resolution and a policy store containing hosts would make credential destination a policy edit. The id is the forge's immutable identifier because the display path is a label that may drift; the provider is a key the REGISTRAR chose and a runner maps to a host of its own, which closes the leak ctx-airspace named: until now FlightRepos.From(uri) set Provider = parsed.Host - a mutable slug and a host lifted out of whatever URI opened the flight. With the registry, the control plane refuses a flight whose intent names an unregistered repository (pointing here), and resolves provider and identity through the registered entry; the intent URI contributes only the slug and the pinned ref. Registration v0 is unrestricted and attributed, the chart's shape - registering widens what every envelope layer beneath can reach, so the record is the safeguard and the widening-owner question stays with ADR-0016's closure set. The runner protocol is untouched: LeaseRepoRef keeps its members, and an old runner keeps working when registrars choose host-shaped provider keys."
  },
  {
    "version": "0.49.0",
    "surface": "f4f1d7314cce7fafbafa9ef9b846b3f936867695aa27c013df6e3ee6161492dd",
    "note": "A registration declares what its repository authenticates with. RegisterRepositoryRequest gains credential - a two-value vocabulary, required or none, nullable on the request with null meaning required, so every registration written before this member existed means exactly what it meant. RepositoryRegistered echoes the RESOLVED mode as a required member, because the reader of a registration must not need the defaulting rule: an absent declaration and a declared required are the same fact and read the same on the way out. The member exists because the 08-24 walk found the flight the old shape forbids, live: the claim demanded a credential reference for every repository, the runner refused an empty secret, and the local adapter refused any secret - so a repository reached over file:// (an air-gapped mirror, a bind-mounted checkout, the local provider's own declared deployment shapes) was declared and not flyable. The exemption is the registrar's assertion, deliberately NOT the control plane learning what a provider key means - a key stays the registrar's word and the runner's resolution, and 0.48.0's reasoning for keeping credentials and hosts out of the registry is untouched: no credential and no host cross this wire, only the statement that none is needed. The runner protocol is untouched - a lease for a credential:none repository simply carries no reference for it, which every runner already handles as the ordinary shape of a repo whose secret it was not asked to resolve."
  },
  {
    "version": "0.50.0",
    "surface": "54a52da9ec63527e9842338ab439bf0f19786c4775fe11c978b147bb0a55fc27",
    "note": "The operator table completes, and the composer owns the canonical order. preserve-unadmitted takes and - ADR-0014's one undecided operator, decided 2026-08-24 by naming the governed quantity, reach: true means unadmitted work leaves the machine for a fetchable remote, false means it does not, and 'and' makes composition the meet of that two-point order, so a layer can only ever remove reach. The 'destroying work under review is its own harm' reading loses because the field never destroys anything - the work stays in the tree on the machine that ran it, which is what every envelope meant before the member existed; null stays false, the tight end, not 'no opinion'. The exemption is DELETED rather than reworded, because the drift guard is the stronger custodian: a field with an operator is inside the sweep, a field with an exemption is beside it, and the composer's own constructor refuses a field carrying both. Compose's behavior over destinations is unchanged - the sets still arrive wholesale from the base layer, and the operator is read today by direction comparison (the next version's business) and by composition the day destinations compose element-wise. Also here: Compose unions obligations in layer-name order rather than caller order, discharging the IOU written into the one caller that pre-sorts - composition was always order-free in content, but the composed obligations keep an order and a pin's digest is over the bytes, so two callers composing the same layers must hash the same bytes. Byte-identical for that caller; any caller that did not pre-sort was already producing digests nobody could reproduce."
  },
  {
    "version": "0.51.0",
    "surface": "d2d3c6c3c1630e6289a4bfc91b6bde0574186b059904326fee8a552e5a5a017b",
    "note": "Direction becomes computable: a total two-answer comparator over envelope documents. EnvelopeDirection.Widening answers null - the proposed version is shown to sit at-or-below the applied one under the declared merge operators - or an EnvelopeWidening naming the first field that could not be shown to tighten and both values that moved. There is no third answer, because a caller handed 'incomparable' will argue it into 'unchanged', which is how a constitution bump walks through the gate a floor exists to hold: incomparable, unordered and unknown all take the widening path (Article XI - undecidable halts, never evaluates permissive). This does not reopen 0.31.0's ruling that general widening detection over predicates is undecidable: the comparator never reads a predicate's meaning, it reads the operator table - finite per-field data - and wherever the table declares no order (a root-only scalar, an obligation's body, an id-set change) it does not decide, it answers widening; the undecidable region is mapped to the refusal-shaped constant, not solved, and add-only remains the composition rule restated as direction: to require more than an obligation asks, add one of your own. Scope is one document against its own predecessor under one name - every operator is a meet, a union guard or an equality guard, meets are monotone, and composition already refuses cross-layer moves - and genesis is deliberately the caller's: a first version has no predecessor, and a comparator that invented an empty one would be answering a policy question in a math costume. The totality is guarded both ways in the static constructor: a new composed field fails the build until its direction is decided, and a new operator value fails until the comparator learns to order it - the drift guard, a third time."
  },
  {
    "version": "0.52.0",
    "surface": "8af6cbb30c754d9115346f9cabd8fe284fcd1ce94856f9286506eefbfdf01149",
    "note": "The widening gate is designated, and the comparator learns the discharge pair. AttachmentConditions gains 'envelope widens' - ADR-0016 section 6's gate, with no new primitive: an ordinary check: human obligation carrying this condition is the widening gate, attached from the RECORDED direction of a proposed change rather than from a fact about the work; registrations ride the same form because a new name is reach that did not exist. Validate refuses the form on a check: machine obligation where the author can still act - a machine predicate reads facts, an envelope-change flight ships none, and the pair would be a gate no evaluation can ever open: the permanent deadlock the no-break-glass rule refuses to build. EnvelopeApplied gains widens/flight/awaiting - member additions, null on every apply that lands or is refused, carried when the widening path diverts to its flight, so an older reader keeps reading applies and a newer one learns where the gate went. And one refinement 0.51.0's first consumer found: Validate requires every obligation be discharged, so adding an obligation forces adding its discharge, and a direction rule that widened on any discharges change made 'add an obligation through a full document' impossible - the flight layer's ordinary day. A discharge gained for an obligation new in the same change now rides its tightening; one gained for a pre-existing obligation still widens, because rewiring who answers an existing gate has no declared order."
  },
  {
    "version": "0.53.0",
    "surface": "17d1bd9d0ed1bfd1ebb3e6ec8b943f75170d852020be46ecc814fef91a11ecd0",
    "note": "Registration becomes gateable, and the doors say so. The three registration endpoints - POST /v1/environments, /v1/airspace/names, /v1/airspace/repositories - gain 202: the registration widens what the tenant can reach (a new name is reach that did not exist, ADR-0016 section 6 arriving where slice eight's 'unrestricted and logged' pointed), so it may ride a flight, and the answer names the flight, who the gate awaits, and what widens. The pending body is its own pinned record, RegistrationPending, declared per-endpoint through the surface's new PendingResponse slot rather than layered on the done shapes - EnvironmentCharted, TopologyName and RepositoryRegistered carry required members a pending answer cannot honestly fill (who registered it and when, which have not happened yet), and a done shape with the truth left blank would teach every reader to distrust required. All three members are required because a registration is a widening by definition: direction is a constant of the act, recorded rather than computed, so there is always a field to name, a flight to point at, and an approver the gate awaits. DestinationKinds gains airspace-registration - the envelope-change shape again, because admission must block on the widens-designated obligations of the envelope in force, so a registration cannot land past its own open gate. Absence still means no, the envelope door's rule: a root that declares no widens gate refuses every registration naming the missing obligation and the tightening that adds it, so v0's unrestricted-and-attributed posture retires where this ships. 200 remains for the one registration that widens nothing - a name already present answers done, because reach that already exists cannot be widened into being."
  },
  {
    "version": "0.54.0",
    "surface": "a91981ffe15990af93c371db278d8dd8b99f4546ca6b03013b7984a0108d34a4",
    "note": "The reason vocabulary: why something did not happen crosses as data, and the sentence derives from it. Slice eight's waiting sentence was a string built control-plane-side and pattern-matched by clients and scripts - wording as protocol, undeclared, so a reworded surface was a silent break nobody chose. Reason is the declaration: a closed kind (ReasonKinds, fingerprinted), the params the grammar reads, and the family the kind derives (ReasonFamilies: declined - a person said no; failed - nobody said no, the world cannot satisfy it; refused - the rules said no). Reason.Sentence is the ONE grammar, contract-side, byte-compatible for the waiting sentence so nothing a person reads moved - and Sentence and FamilyOf THROW on a kind nobody declared: a renderer that shrugs files a governed refusal under healthy, Article XI's exact shape, so the gap fails a build or a render, never an audit. Validate refuses a stored family that disagrees with its kind - derivable and carried anyway, because a reader filtering refusals from waits should not need the kind table, and a disagreement is a lie one of them is telling. FlightSummary.Waiting and ChecklistItem.WhenUnmet keep their wire NAMES and change type to Reason - the loud break, chosen deliberately over a rename: an old reader of a renamed member sees null and renders every waiting flight as healthy, and a type change fails fast on both sides instead. The declined family ships without a kind: its first kind arrives with the producer that mints it, because a constant nothing produces is a promise nobody has to keep. Five kinds ship - no-runner-advertises (failed); cannot-be-shown-to-tighten, widening-requires-a-gate, uncharted, registration-is-a-widening (refused) - each with the sentence its consumers already assert."
  },
  {
    "version": "0.55.0",
    "surface": "8f680997504b5432deb31d2a3be1dfd8534f123aecc11b61467fb46790ae4d83",
    "note": "The attach history crosses: when an obligation's answer changed, both directions, with times. The attribution endpoint kept only the newest answer per obligation, so a gate that appeared and vanished read as never-asked - and ADR-0014 use case 14 says a gate that appeared and vanished is exactly what a reviewer needs to see; hiding the middle would file a withdrawn question under never-asked, which matters most on the flight where the estate asked and then stopped asking. AttachmentTransition is {to, at, because}: to is the existing Attachments vocabulary (no new state - a fourth would make the history unreadable, and Validate refuses one), at is when the evaluation that changed the answer ran, because is the engine's reason for the new answer. ObligationAttribution.Transitions carries state CHANGES only, oldest first - the first evaluation is the first entry - so the common case stays one entry and the wire stays small. Defaulted empty rather than required: the history is DERIVED server-side by folding the attribution stream the control plane already appends (no new event, no client recomputation), which also makes it retroactive - a flight that flew before this member existed reads its own history - and an older control plane serving none renders as nothing rather than as a claim about times nobody recorded. This is slice eleven's reader shipped with (as) the writer: slice seven closed owing a reader for a staleness flag, and a value nothing consults is not a warning."
  },
  {
    "version": "0.56.0",
    "surface": "3b6a02229d49778704104e1fe0168666ff616ea48ef93001908247118cfb0647",
    "note": "The act crosses as a condition: moves used include <move>, the first when-form whose subject is what was DONE rather than what the work contains. It reads the flight-wide union of every loop.outcome's recorded moves - monotone by construction, an act cannot be un-happened - so a gate attached by it does not detach when the evidence of the act is deleted. This is ADR-0014's laundering attack closed before the exposure exists: run-migrations is not a move yet, and on the day it arrives the obligation that gates it reads this form or it is precisely the hole (write a migration, run it, delete the file, watch the gate detach). The authoring rule rides the form: if the concern is that something happened, the condition must read a monotone fact - the touches form beside it reads the work as it stands, and slice eleven classified and split the two on the control-plane side. Unlike the glob, the move value is the closed LoopMoves vocabulary, so Validate refuses a typo where an author can still act instead of shipping a gate that silently never attaches - the one hazard the touches form cannot avoid. Allowed on either check kind: it reads a measured fact, so the deadlock that bans machine-check widens gates does not apply."
  },
  {
    "version": "0.57.0",
    "surface": "b00fce8959c54509370cb94ab063f2f5c425cee142efc8f8d48f110081daf571",
    "note": "moveEnforcement becomes a measurement, and the fact says when. environment.identity gains probedAt (fact vocabulary 0.16.0), and two members change meaning without moving: moveEnforcement now derives from the SESSION's own probe - a held bound proves per-tool; a broken or unmeasured one ships nothing, because that lease is released with the probe's diagnosis instead of flying ungoverned - and movesProbed is the set the session's probe actually held (Edit and Write, each checked against its own artifact, after slice eleven's step 0 caught substitution live: an agent denied Edit modified the file with a granted Write, so one canary answers one tool). The probe runs before every invocation now, under the lease's own renewal: ambient settings act on the session, and the family has five members (--permission-mode acceptEdits defeats the bound even with setting sources cleared, measured 2026-08-25 on CLI 2.1.243). This is slice two's moves row taking its third correction, upward, on a probe rather than a flag - which is the difference that made the first correction wrong. full and none stay in MoveEnforcements as the values whose absence is the finding: full is never derivable from finitely many refusals, and none never crosses from a working runner. The stale prose fell with it: LoopMoves' 'not enforced', MovesUsed's 'never enforced', and RefusedMoves' 'the allow-list does not bind' each now say what the measurements found."
  },
  {
    "version": "0.58.0",
    "surface": "52060af963b470977ad70d3d80a8fa2686519c92cd5a2d429a7ad9afacdf5d0f",
    "note": "Every move bears a kind, and an outward act nothing can confirm is refused at authoring. ADR-0014's third resolution: enforce a move whose use is itself the outward act; record-only a move whose product a destination still gates. MoveKinds classifies all five declared moves record-only - read and search produce knowledge, edit and write produce tree state the manifest measures, run-tests produces outcomes the verdicts read - so THE ENFORCED SET IS CORRECTLY EMPTY, asserted as the ratchet's anchor rather than left as an implication. MoveKinds.Of THROWS on an unclassified move (the Reason.Sentence shape: a sixth move fails a build or a validate, never an audit, because the silent default would be record-only and record-only is the answer that grants what nothing can recall). Validate refuses any declared outward-act move naming it: at authoring there is no runner and no probe, and the honest question is whether anything this product has lets a probe confirm enforcement of a GRANTED move's use - nothing does; the probe confirms withholding of tools that were not granted, the opposite bound. The refusal is exercisable today with zero outward moves, through a public Validate overload that exists so a planted classification can prove the branch alive - the lock installed before the door, because the first outward moves (send; power-on and power-off with the maintenance verbs) arrive into a platform that already refuses to author what it cannot enforce. Contract-fingerprinted, not fact-fingerprinted: the kind never crosses in a fact."
  },
  {
    "version": "0.59.0",
    "surface": "19d6cbc62403f9a73b278a3b9e06d25c484f40c23546dea8ff5cb75038dbbf6e",
    "note": "The strategy document arrives, and an accepted decision stops being prose. ADR-0015's management half needs a document a tenant can author; this is its shape: EnvironmentStrategy - kind (StrategyKinds, closed at docker-host because the ADR's eight rows are eight infrastructures and only one has to exist), the charted environment the pool furnishes, inventory (pool + size, the scope section 12 binds the runner's credential to), pull-point (PullPoints, closed at resident-runner; a strategy naming none is refused at authoring because a powered-off pool cannot pull), an image pinned by digest (what reset resets TO must be a fixed point), and bounds (pool-max, active-hours - no spend ceiling, deliberately: docker-host meters no spend, and a bound nothing measures is a promise nobody has to keep). NO HOST, NO SOCKET, NO CREDENTIAL, BY SHAPE: the resident runner's endpoint lives in its own environment and nowhere on the wire, held over the member types the way the repository registration's rule is. Roles gains strategy - a role that never composes and that no flight can be FOR - and the document applies to its topology name through PUT /v1/airspace/strategies/{name}, answering the envelope's own EnvelopeApplied because it rides the same per-name stream and version counter. Riding along: DestinationKinds.All gains the airspace-registration member it declared in 0.53.0 and never listed - slice twelve's step 0 found the kind refused by the very vocabulary that declared it, a value made unreachable by its own list, repaired here so PoolActions cannot inherit the shape by example."
  },
  {
    "version": "0.60.0",
    "surface": "6b5ea08150c74081f63320f4e0ba28fb7f8b019cbe7296ab84da91b739429a78",
    "note": "The pools surface: a routine action attests, and the attestation is not a fact. The fact plumbing is lease-welded at four independent points - FactBatch carries only a generation, the ship endpoint is scoped to a lease, ShipFactsAsync takes a lease id, and the idempotency key needs a flight id - and a verify, refresh or reset has no flight, so grafting a flightless path under the fact name would import nine kinds of machinery to dodge one record. PoolAttestation gets its own record and its own governed prefix instead, and KindsThatCross stays NINE, asserted deliberately in the slice guard. PoolActions closes at three (verify inspects; refresh makes a member current and running - warming IS refresh; reset destroys and recreates from the strategy's pinned image), every member mirrored into All because a declared value outside its own membership list is the DestinationKinds hole step 0 found. PoolActionKinds is MoveKinds' shape, second instance and deliberately its own table: refresh and reset are outward acts whose product faces no destination gate, verify is record-only, and the enforcement consumer is the control-plane decider - an outward act is decided only toward a pool whose latest attestation carries a current scope probe, unknown is not false. MoveKinds' enforced set stays correctly empty: slice eleven's pre-booking half-arrives, the work kind without the loop move. A failed attestation must carry its diagnosis, because the failure is the discriminator between the two maintenance tiers and a failure that cannot say why escalates nothing. The pull point serves decided actions runner-audience, and serving is the claim: two residents polling one pool get disjoint sets. EnvironmentProvenance's 'warm pools are years away' prose retired: the field became the feature it was waiting for."
  },
  {
    "version": "0.61.0",
    "surface": "3a5e0a60734adbf0efb5ba6a7197edaa5b3b103e8ea38aaebee158618077f01f",
    "note": "Bounds bind, and a decline never wears a gap's clothes. The declined family's first kind arrives WITH ITS PRODUCER, exactly as 0.54.0's ledger reserved: blocked-by-bound, minted by the control-plane decider (which declines by not deciding) and rendered by the read models from the same inputs. Params [bound, clearing] or [bound, clearing, eta]: the bound is the strategy field's own name, the clearing one of BoundClearings - capacity (clears when a peer flight releases) or schedule (carries when it opens; a schedule with no eta is refused, because a wait with no end a reader can plan around is not a schedule). An UNKNOWN clearing throws, one param deeper than an unknown kind - the render can never blank, and the authority clearing stays deliberately absent until the first metered strategy row arrives to produce it. pool-warming joins as failed: the managed-and-warming state is neither a gap nor a bound, and without its own kind it would have to wear one of their clothes - 'no runner advertises environment=X' and 'declined by your own bound' are the same silence with opposite remedies, and reporting the first when the second is true sends a customer to buy hardware to fix a number in a document they already own. ChecklistSatisfiers gains declined-by-bound, the third value slice eight's closure comment named as the design event that would mean a strategy exists; the comment now records the event arriving. The maintain-environment work kind is control-plane topology, not contract surface - nothing here moves for it."
  },
  {
    "version": "0.62.0",
    "surface": "3a5e0a60734adbf0efb5ba6a7197edaa5b3b103e8ea38aaebee158618077f01f",
    "note": "A name is now also a path, so the one vocabulary ADR-0014 deliberately opened narrows deliberately in turn. AirspaceNames.Invalid is the single computation both repositories consume - lower-case letters, digits and dashes, no leading or trailing punctuation, 64 characters, and the separator refusal that already existed kept for its reason rather than its effect. Case is refused AT THE NAME because the filesystem will not refuse it: 'Payments' and 'payments' are two streams, two version counters and two topology rows, and one file on macOS and Windows, and every comparison downstream is ordinal over a hash of the name's bytes, so nothing can recover from the collision once it exists. The surface hash does not move because the fingerprint covers pinned types and their members, and this is a rule rather than a document - which is itself worth recording, because it means the guard that usually forces this conversation could not have forced this one. PathFor and NameFrom ride with it: the mapping is the identity function, refused a manifest because a manifest would be a second source of truth about identity, free to drift from the streams that actually use it. Measured before it was decided - twelve candidate names declared against a live control plane, eleven accepted, '../../etc/passwd' among them, and only '@' refused. Reservedness is NOT here: root, flight and the shipped work kinds are valid as shapes and undeclarable as a rule, because a reader spelling root@v7 still has to be able to spell it."
  },
  {
    "version": "0.63.0",
    "surface": "d611535ab53860c1aa261ac2b7a179b771e98ad39cd9bbaefa84800e59626745",
    "note": "The named document door, and the end of a deferral that had slipped three times. GET /v1/airspace/envelopes renders the estate, GET and PUT /v1/airspace/envelopes/{name} read and apply one document. Until now only root and strategies had doors: a work kind or a narrowing could not be read back, could not be applied, and its widening was refused outright because there was no flight for it to ride - which made 'retire and redeclare' the only path and made retirement the thing you needed first. ADR-0016 draws a tree with four directories and two of them were unwritable, so this is the floor the working copy stands on rather than a convenience on top of it. NamedEnvelopeApply is a wrapper rather than two endpoints because the topology already decides which shape a name carries, so a body naming the wrong one gets a sentence about THIS name instead of a 404 on the endpoint the caller did not pick; its Validate refuses both-and-neither, and an empty body is refused rather than read as a retirement, because retiring is a terminal version and not an absence. What is deliberately NOT a member is the based-on: precondition, which travels as a query parameter: the stored form of the body is the idempotence key, its field-by-field comparison decides whether an apply gates, and its bytes are what the composition digest hashes - so a member that changed on every pull would mint a version per document per pull AND divert every one of them to a human gate."
  },
  {
    "version": "0.64.0",
    "surface": "d611535ab53860c1aa261ac2b7a179b771e98ad39cd9bbaefa84800e59626745",
    "note": "The estate can be written down. EnvelopeText gains its third render path - a strategy - and until now there was none: a strategy could be applied and never read back as text, so strategies/, one of the four directories ADR-0016 draws, could not be emitted by anything. The surface hash does not move because rendering is a computation over types that were already on the wire; what moves is what the tool can do with them. ADR-0013 is explicit about what an unproven round trip costs - Render omitted Obligation.Evidence once and show -> apply stripped a governance declaration with nobody noticing - so this path carries parse(render(x)) == x from its first commit, with a poison twin per key and a ratchet over the schema, because at estate scale the same defect is every document a tenant has rather than one. An absent bound stays absent: writing active-hours: with nothing after it would parse back as a bound nobody declared, and a bound nobody declared is a wait nobody can clear. EstateRenderTests is the census that would have made the gap loud in the first place - the suites covering rendering each name their own type, so a type nobody named was covered by nobody, silently."
  },
  {
    "version": "0.65.0",
    "surface": "d611535ab53860c1aa261ac2b7a179b771e98ad39cd9bbaefa84800e59626745",
    "note": "The working copy's precondition. ReasonKinds.StaleWorkingCopy is the first reason kind about a PRECONDITION rather than about a document, and it names both versions because the useful question is not whether you are stale but how far: one version behind is a pull, eight is a colleague to talk to. The named apply gains 409 to carry it. based-on: travels as a QUERY PARAMETER and never as a body member, and the reason is mechanical rather than aesthetic - the body's stored form is the idempotence key, its field-by-field comparison decides whether an apply gates, and its bytes are what the composition digest hashes, so a member that changed on every pull would mint a version per document per pull, divert every one of them to a human gate, and move every pin at once. That is slice nine's contract-bump minting hazard walking back in through a new door. The parser gains a THIRD class of key to match: part of the document, refused, or consumed. provenance: is refused because the composer assigns it; based-on: is honoured because the applier states it - asserting where you stand versus asserting what you saw. The surface hash does not move because no wire type changed shape; the endpoint fingerprint does, because a status did."
  },
  {
    "version": "0.66.0",
    "surface": "d1657ea06128f1b7e55386976f8411c7013f95b7250d420fbdd3cc4e8ed89907",
    "note": "stale-working-copy joins ReasonKinds.All and FamilyOf, which 0.65.0 shipped it without - and the surface moves BECAUSE of that, since All is what the closed-vocabulary fingerprint reads. A kind missing from All is invisible to the guard whose entire job is noticing a new value, which is how this got past a green suite: nothing in this repository calls FamilyOf on a kind it did not name explicitly, so the only consumer that ever asked was the control plane, and it asked in production shape - the reason was built, FamilyOf threw, and a governed refusal came back as an internal error. That is the exact failure Reason's own doc comment says these throws prevent, one level up: the gap failed an audit instead of a build. ReasonFamilyTotalityTests closes it by walking All rather than naming kinds: every declared kind must derive a real family and render a sentence that is not its own machine word, and an undeclared kind must still throw. The next kind is covered the day it is written rather than the day somebody consumes it."
  },
  {
    "version": "0.67.0",
    "surface": "688d37ca669cddad503f517e52bc76c51b1815ff5112683021d622679fdf9206",
    "note": "Retirement, as a version rather than a deletion. ADR-0014 is explicit and the alternative is what makes it explicit: retiring by deleting a topology entry is a governance-critical change wearing bookkeeping's clothes - the constraint stops attaching and no version anywhere records that it did. So POST /v1/airspace/envelopes/{name}/retirement writes one more version to the name's own stream, and the topology reflects it rather than deciding it: unreachable becomes derived. There is deliberately NO 200 in its status list, because a document that stops applying removes every constraint in it at once - retirement is a widening by construction, which is registration's rule in the other direction, direction as a constant of the act rather than something computed. ReasonKinds.FlightsInTheAir is the one refusal direction cannot supply: retiring a NARROWING is a widening and rides its gate, but retiring a work kind is not obviously a widening at all - removing the sets makes less possible rather than more - and its real hazard was never direction. It is orphaning a flight already declared against it, so the refusal names the flights. It joins ReasonKinds.All in the same commit that declares it, which is 0.65.0's lesson applied on the way in rather than after a consumer found it as a 500."
  },
  {
    "version": "0.68.0",
    "surface": "094d4c5ddaeb03f5b8ecfdf35b301a3fcb2d587b59c43190cea6bb694701b29f",
    "note": "Flights end, and the lease is not what ends them (ADR-0017). FlightStates is the vocabulary this platform did not have: before it, termination was a property of the LEASE - in the air meant the newest lease's disposition was not terminal - so the three work kinds that are never leased were in the air permanently, and a governance act that had already applied was indistinguishable from one somebody was still working on. Measured before the fix: on a stack that had only run the estate walk, eleven of eleven flights were permanently in the air and zero leases had ever been issued. Six states, four of them exits. The two that are not exits are the load-bearing part: `open` is the absence of an ending and `unknown` is the absence of a record, neither happened TO a flight, and a work kind declaring `unknown` reachable would be declaring that its flights can end in the state meaning nobody knows how they ended. Article XI is why `unknown` is a value at all rather than a null - the defect being fixed is `disposition IS NULL` reading as in-flight, and the cure is not to flip it, because a terminality query that quietly matches nothing returns the empty queue this whole design is trying to produce. There is deliberately NO state for halted, exhausted, abandoned or expired: those flights are STOPPED rather than finished and are resumable by anyone from anywhere, which is the product's central claim, and ending and stopping are different axes. Derived like RunnerStates and reported by nobody - nothing on the wire may set a flight's state. FlightSummary does not gain its member here: it gains it with the producer and the reader that populate it, rather than sitting unpopulated for six steps. The surface moves because All is what the closed-vocabulary fingerprint reads, and Exits moves it a second time, which is correct - the exits are a wire vocabulary in their own right and a work kind declares against them. Also deletes ChangeKinds.DestinationLanded: a FACT kind declared inside the per-path change vocabulary, duplicating FactKinds.DestinationLanded, correctly absent from that class's All and therefore invisible to every fingerprint, and referenced by nothing in either repository. Found by ClosedVocabularyTotalityTests, which generalises ReasonFamilyTotalityTests from one vocabulary to all of them: 0.65.0's stale-working-copy defect - a constant that never reaches its list is invisible to the guard whose whole job is noticing new values - is not specific to reason kinds, and every vocabulary here could fail the same silent way."
  },
  {
    "version": "0.69.0",
    "surface": "73bab10a8e044c4eff6151a59bb6d62e13837b41822838121f986475770478ba",
    "note": "A person may withdraw a flight, which is ADR-0017's second open question closed the harder way. The ADR asked whether only the SYSTEM may reach the withdrawn exit; if so it gets counted callers and nothing else. A person may, so it needs a door - POST /v1/flights/{ref}/withdrawal, Developer, carrying a reason and nothing about who. Article XII derives the actor from the authenticated principal, and a caller naming somebody else would be a caller choosing its own attribution, which is the one thing attribution may never be. The reason is REQUIRED on the wire rather than by convention, and that is the whole guard: 'the question ceased to apply' is the most reachable sentence in the terminal vocabulary and fits almost anything somebody finds inconvenient, so having to say which question is what stops withdrawn becoming a garbage collector - a politely optional member would be omitted by the first client in a hurry. No 200, the retirement door's arrangement one estate over: the answer is that the flight is over and what a caller does next is read it. 409 for a flight that has ALREADY ended, refused rather than silently accepted, because accepting would let a withdrawal appear to rewrite an ending that already happened. This deliberately changes what the totality guard means one repository over: with a person able to withdraw ANY flight, counting that toward a work kind's ability to finish would make ADR-0017's build failure unfailable, so a kind declares what the SYSTEM can reach for it and person-operated exits are named separately. The surface moves for the new pinned type; the endpoint fingerprint moves for the route."
  },
  {
    "version": "0.70.0",
    "surface": "9e0f0a80cdb445bb0a41d2ecd80cd2e487f43347fd92edface0bbea8b260e03a",
    "note": "The queue means what its description says. FlightSummary gains `state`, one of FlightStates.All, and GET /v1/flights gains ?all= - held back from 0.68.0 on purpose, so the member ships with the producer and the reader that populate it rather than sitting unpopulated for six steps as a registered-not-invoked. Derived control-plane-side like RunnerSummary.State and reported by nobody: it is a function of what was recorded when the flight ended and of what can be derived at read for flights that ended before there was anywhere to record one, and nothing on the wire may set it. DEFAULTED rather than required, which is the one decision here worth arguing: a control plane older than this answers without the member, and requiring it would make every such flight fail to deserialize - taking the queue away entirely in order to add a column to it. `unknown` is the honest reading of a flight from a build that could not say, which is the same sentence the member exists to make sayable. ?all= is a parameter rather than a second route because the question is the same one; what changed is that there is now an honest default answer to it, and the endpoint gains 400 for a value it cannot read. `unknown` stays in the DEFAULT view deliberately - a flight nobody can account for is exactly what somebody should see, and hiding it would be the empty-queue-reads-as-health failure this slice is most afraid of. Both fingerprints move and they are different numbers: the contract surface for the member, the endpoint surface for the status."
  },
  {
    "version": "0.71.0",
    "surface": "9e0f0a80cdb445bb0a41d2ecd80cd2e487f43347fd92edface0bbea8b260e03a",
    "note": "A narrowing constrains, and until now it did not. Destination.Requires has carried [Composes(MergeOperators.Union)] since slice nine and Compose never applied it: destinations arrived wholesale from the base layer and no lower layer touched them, so a narrowing's obligations were composed, evaluated and recorded, and admission - which iterates a destination's requires and nothing else - never looked at them. ADR-0014 already named the cost in the sentence 'a narrowing must be able to union `requires`, or it is decorative', and the drift guard could not catch it because the guard asserts that every field DECLARES an operator rather than that every operator is APPLIED. Compose now unions every obligation a `narrowing`-role layer contributes into EVERY destination's requires. THE BINDING IS THE COMPOSER'S rather than the author's, and there is deliberately no member on EnvelopeNarrowing naming a destination: such a member is a cross-document reference that can dangle - naming an exit the work kind does not declare, or failing to name one it does - and a type with no such member cannot express either failure, which is the argument ADR-0014 already makes for scope and constitution one field over. Every destination rather than the first, because a narrowing constrains the flight and not one exit from it; binding to Destinations[0] would let a tenant with two destinations admit around a compliance gate. ROOT AND THE WORK KIND ARE NOT AUTO-BOUND: they author their own requires in the same document, and 'a destination requiring nothing is a real envelope' is a choice a floor's author made deliberately, while a narrowing has no field in which to make it. The per-flight overlay binds too, because it is role `narrowing` - a gate that attached mid-flight and cannot refuse the destination is the laundering shape arriving through the composer instead of through a deleted file. NO WIRE MEMBER MOVES and the surface fingerprint is unchanged; the version moves because the meaning of a document a consumer already holds has changed, which is exactly the thing a consumer reading this number needs to be told."
  },
  {
    "version": "0.72.0",
    "surface": "ea681071d2631176bb7fee599d5b5830dfad5f3ee5ff8176285d43edbcb38571",
    "note": "Airspace decides whether a repository may speak. RegisterRepositoryRequest gains `narrowings`, the directory in that repository under which every document is a narrowing (ADR-0018 \u00a7 2 and \u00a7 7), and RepositoryNarrowings is where the rule about it lives. NULL IS OFF AND BLANK IS A REFUSAL, which is the member's whole difficulty: null is a decision not to declare and is every tenant today, while empty would be a declaration of nothing - narrowings at the root of the tree, every file in the repository read as policy - so a client sending \"\" for an absent field would turn the tap on. A DIRECTORY AND NOT A FILE, refused by the trailing slash, because CODEOWNERS grants per path: two concerns in one file share one owner, and one owner over two concerns is what makes CODEOWNERS decoration rather than the control this whole layer rests on. A path containing '..' or a leading '/' is refused NAMING WHAT IT OBJECTED TO, because this declaration is the only bound on what the control plane will ever ask a forge to read - a forge has no path-scoped permission, so a token that can read one file can read every file and the containment is this rule or it is nothing. A backslash is refused rather than normalised: a forge serves forward slashes, and normalising would make a declaration mean one thing where it is written and another where it is fetched. DECLARING IT IS A WIDENING WITH NO NEW RULE - ADR-0016 \u00a7 6 already says a registration is reach that did not exist a moment ago, so it takes the same gate registration always took, in both directions. And NameRuleTests' single-decider scan is scoped rather than dodged: RepositoryNarrowings.Invalid decides what a PATH may be and AirspaceNames decides what a NAME may be, so it rides a written exemption with a staleness check beside it, rather than being renamed out of the scan's way."
  },
  {
    "version": "0.73.0",
    "surface": "2f51d632df7ebaf5c6dfb15608740ffad505cf87711127a7ffd1f9da2c3f03e9",
    "note": "A registered repository reports its declaration. RepositoryRegistered gains `narrowings`, so the tap a tenant turns on is one they can read back - registered is not invoked, and the whole of ADR-0018 \u00a7 2 rests on a person being able to see which repositories may speak. It is NULLABLE on the answer, which is deliberately not the arrangement Credential has one member up: there an absent declaration and a declared `required` are the same fact, so the answer is required and a reader never needs the defaulting rule; here absent and declared are DIFFERENT facts - off versus on - so the absence has to survive the round trip as an absence, or a reader cannot tell a repository that is not governed from one whose every file is policy. And whatever the answer carries for off must itself be a legal declaration to send straight back, asserted directly, or a round trip through pull and apply refuses what it was just told."
  },
  {
    "version": "0.74.0",
    "surface": "2f51d632df7ebaf5c6dfb15608740ffad505cf87711127a7ffd1f9da2c3f03e9",
    "note": "`narrowings` is declared on the wire. ProtocolSurface.JsonMembers gains it on both RegisterRepositoryRequest and RepositoryRegistered, which is what actually makes the member exist as far as the two repositories are concerned: they cannot reference each other, so this table is the only thing holding them together on member names, and the control plane's conformance test compares what it serializes against this list and nothing else. IT IS ALSO HOW THIS WAS FOUND - the member was added to both records, the registry, the schema and the door, and the control plane answered 'got: credential, id, name, narrowings, path, provider' against a declaration of five, one repository and one release away from the line that needed changing. NEITHER FINGERPRINT MOVES: JsonMembers is a declaration table rather than part of the hashed surface, and no route or status changed. The version moves anyway, for 0.71.0's reason - what a consumer is required to serialize has changed, and a consumer reading this number is exactly who needs to be told. A general guard rides with it: every member of both records must be declared and every declared member must exist, so the next one added to either fails in this repository rather than in the other one."
  },
  {
    "version": "0.75.0",
    "surface": "2f51d632df7ebaf5c6dfb15608740ffad505cf87711127a7ffd1f9da2c3f03e9",
    "note": "THE PARSER MOVES INTO THIS PACKAGE, AND IT IS A REVERSAL. EnvelopeYaml was in Gg.Client and this project carried ZERO package references, asserted by ContractsDependencyTests because this is the public artifact a customer audits and should not inherit anyone else's framework - and by a test named 'the emitter is here and the parser is not', for the split that let the schema live in an assembly with no dependencies: model -> text is deterministic and hand-written, text -> model needs a grammar, and every grammar is a package. Both were right about the cost and both are amended rather than deleted. ADR-0018 \u00a7 5 makes the control plane parse a repository's narrowing ITSELF - a runner reporting 'no narrowing here' is a silent weakening and Article IX admits no exception - and the control plane references this package and nothing else. Two alternatives were weighed and declined: publishing Gg.Client as a second release asset, and a parser-only third package. WHAT IS BOUGHT is parse and render in one assembly, which is what ADR-0013's round trip has always wanted; their being in two is how `evidence:` stayed authorable, load-bearing at the gate, and emitted by neither path for three contract versions. WHAT IS PAID is that a customer auditing this package now sees YamlDotNet. The guards narrow rather than vanish: exactly ONE package reference, NAMED so a swap fails too, and exactly one parser-shaped type, so a second grammar is still the drift the original test was protecting against. The parser's result types sit in Gg.Contracts.Authoring, excluded from the pinned-id and vocabulary rules for the reason Gg.Contracts.Description already is - nothing in either is ever serialized, so a pinned id would be a promise about something that never crosses a boundary - with a staleness check that an excluded namespace holding nothing is an error. AND `gg envelope validate` LEARNS THE ROLE: it reads a document as what its containing directory says it is, and NAMES the role it read. That is ADR-0018 \u00a7 7's fourth refusal, the one that gets skipped because the document is not malformed but misplaced - a complete envelope copied from root.yaml into a narrowings directory parses, validates, and would hand a team `scope:` and `constitution:` through a merge nobody gated."
  },
  {
    "version": "0.76.0",
    "surface": "2f51d632df7ebaf5c6dfb15608740ffad505cf87711127a7ffd1f9da2c3f03e9",
    "note": "A THIRD KIND OF NAME. RepositoryNarrowingNames composes `<repository>/<filename>` - what a narrowing living in a service repository is called (ADR-0018 \u00a7 7). Namespaced, because without it a team shadows a compliance regime by choosing a filename, which is a widening performed by a merge. THE LEFT HALF IS THE REGISTRY KEY AND NOT THE FORGE PATH: a path is a display label that may drift, and a name built from one would rename every narrowing the day somebody renames a repository on the forge. THE LAST SLASH IS THE SEPARATOR, and that single fact is what keeps the mapping injective: a registry key may hold a slash - a slug is a slug - while a document name may not, because it is one file in one directory. Splitting on the FIRST would parse `acme/widgets/pci` as a document called `widgets/pci`, which cannot exist, silently. A COMPOSED NAME CAN NEVER EQUAL AN ESTATE NAME, free by construction and asserted anyway: an estate name is one path component and a composed one always carries a slash, so shadowing is impossible rather than discouraged and `flight` stays reserved without anybody defending it. The halves are held to DIFFERENT rules on purpose - the left is a slug, the right is handed straight to AirspaceNames so `payments/PCI` and `payments/pci` cannot be two names for one regime on a case-insensitive filesystem. And the one the registry lets through is refused here: a repository key may contain `@` because the registry refuses only blank and newlines, but versions are qualified name@vN, so a key carrying one composes to a name nothing can parse a version out of. NameRuleTests' single-decider scan fires on this and rides a written exemption - with a test that PROVES the delegation rather than claiming it, because a second name rule that merely says it defers is the exact drift that scan exists to catch."
  },
  {
    "version": "0.77.0",
    "surface": "830fe2f1ae58ac801e565baed5f17d90a88ed789d0d7a2ffb6df5b17b14c7298",
    "note": "ABSENT AND UNREACHABLE ARE TWO ANSWERS. ReasonKinds gains declared-and-absent and forge-unreachable, and the distinction is the whole of ADR-0018 section 6. A declared directory that is not there at the pinned commit is a MISCONFIGURATION - usually a sequencing one, the tap turned on before the team committed anything, or the directory sitting on a branch and not at the commit this flight is working from - and the fix is a merge. A forge that cannot be asked is an OUTAGE, with nothing for the tenant to fix; collapsing the two makes a forge incident look like somebody's mistake and sends the person having the outage to check a declaration that is perfectly correct. NEITHER MEANS THE REPOSITORY CONSTRAINS NOTHING, which is the third answer and the one a read that did not happen must never produce: a fetch that 404s and composes as no narrowing is indistinguishable from a repository that chose to constrain nothing, and that is the silent weakening this feature exists to prevent. Both sentences are asserted not to make that claim - and the assertion was WRONG TWICE before it was right, banning first the word 'unconstrained' and then the phrase 'constrains nothing', in sentences that used both contrastively to say the opposite. A guard that bans a word rather than a meaning fails on the sentence that says the right thing; the sentences now say what IS true rather than naming what they are not, and a liveness twin proves the narrowed check still bites. A HALT IS NOT A REFUSAL, so both belong to the failed family: nothing was refused, the flight was admitted and cannot be evaluated, and filing them under refused would put a flight that is WAITING in the same bucket as a document somebody was told no about. Both join All in the commit that declares them - 0.65.0's lesson for the second slice running, where stale-working-copy shipped with a sentence and no entry, invisible to the fingerprint that reads the list and to the guard whose whole job is noticing a new value."
  },
  {
    "version": "0.78.0",
    "surface": "80773954435a361db1ec287ef75167018c2772abb404138760885716dd1d086c",
    "note": "ACCEPTS: IS THE FLOOR BOTH HALVES OF ADR-0020 COMPUTE FROM, and step 0 found it absent along with the half nobody had noticed: THERE WAS NO SUBJECT-KIND VOCABULARY EITHER. What this codebase called a subject was a repository registry name, and a launch request carries a work kind, an environment and a repository as three unrelated optional strings; `repo` was not a value anywhere, so accepts: had nothing to range over. SubjectKinds is minted closed, with one member, because the value this vocabulary exists to make writable today is the EMPTY LIST - a statement `[]` makes and no amount of optionality does. AN UNKNOWN KIND HALTS rather than reading as no constraint: the permissive reading turns a typo in accepts: into a kind that accepts everything, with nothing on the page saying so. SCOPE STAYS REQUIRED AND GAINS `none` AS A VALUE, never becoming optional, because a line that can be dropped is a constraint that can be dropped silently - evidence: was authorable, load-bearing at the gate, emitted by neither render path, and an edit round trip stripped it. And it is a TIGHTENING: `**` was the only way to write unbounded and was indistinguishable from a bound somebody meant, so after this subjectless work says `none` and `**` goes back to meaning every path. THE AGREEMENT IS REFUSED IN BOTH DIRECTIONS, and neither refusal is the other's contrapositive: a kind over a tree that bounds no path would fly unbounded over a repository, and a kind with no tree that bounds a path has written a rule nothing can read, which is a gate that reports satisfied by never running. Both name the field, the value and the kind. THE AGREEMENT IS ROLE-FREE AND THE ABSENCE IS NOT, which is why Validate gains a role-aware overload rather than a parameter: carrying accepts: is itself the claim to be a work kind, so the two fields can be checked against each other with no topology in front of them - reachable from a parse of one file, which is what lets validate catch it before an apply happens. Whether a work kind was ALLOWED to omit the field, and whether root was allowed to declare it, are questions about the role the document was applied to, and the role is never read from the document. ABSENT IS REFUSED RATHER THAN DEFAULTED to [repository], even though that is what every kind before this field meant: the default would put a subjectless kind one keystroke from a kind that takes a tree with nothing on the page saying which was meant, and the whole point of section 1 is that `nothing was written` and `nothing is bounded` must not look alike."
  },
  {
    "version": "0.79.0",
    "surface": "80773954435a361db1ec287ef75167018c2772abb404138760885716dd1d086c",
    "note": "NONE MEETS A GLOB, AND ADR-0020 DOES NOT LIST THE QUESTION. Root is not a work kind: it always applies, it always carries a scope, and composition is a meet - so the FIRST flight a tenant opens for a subjectless kind computes `src/**` against `none`, and nothing defined that. It fell through to `neither contains the other`, which is correct for two globs and nonsense for a glob and a declaration that there is nothing to glob over, so section 1 was shipping the failure section 2 exists to prevent. SECTION 2 DOES NOT RESCUE IT: section 2 is about rules reading fact families the kind cannot produce, and the scope FIELD is not a rule and is not read from a fact - it saves the obligations and leaves the field it was written to protect. DECIDED AS A DOMAIN MISMATCH, which is the ruling section 2 already makes one noun over: a path bound is a statement about a tree, work that accepts no subject has no tree, so the floor's bound is not narrowed away and is not in conflict - it does not apply, decided from the documents alone without evaluating a fact. THE COMPUTED VALUE IS THE SAME AS `none absorbs`, and that is recorded rather than glossed: what differs is not the string but that the answer comes from a declaration rather than from a claim about which of two bounds is smaller. The permissive reading is the same bytes and a different argument, and the argument is what the next person reads. AND THE TRAP THE MEET RULE OPENS IS CLOSED IN THE SAME RELEASE, found while writing the test for it: with `none` absorbing, a ROOT that said `scope: none` would bound every path-taking kind in the tenant to nothing - QUIETLY, where before the meet rule it produced a refusal. So `none` is now legal exactly where `accepts: []` is, which also makes the composer's none branch unreachable except by a document that DECLARED it takes no subject: the reasoning is checkable rather than merely stated. THE WIRE SURFACE IS UNCHANGED and this is still a version, because a consumer pins an exact one and composition now answers differently for inputs it already accepted - nine earlier entries share a surface with their predecessor for the same reason."
  },
  {
    "version": "0.80.0",
    "surface": "d611969526ba6dec149ad5ddc81e473127e3269d07a7c902c9bdb77acfc34d7c",
    "note": "AN ENVELOPE IS A SUBJECT, AND IT HAS NO TREE. SubjectKinds shipped in 0.78.0 with ONE member and an argument that the value the vocabulary existed to make writable was the empty list. ADR-0020 section 1's table has THREE rows - `[repo]` takes a path glob, `[]` takes `none`, and `[envelope]` takes `none` because THE SUBJECT IS THE BOUND - and I had read the section through a slice note rather than the text. So `none` is not the value for NO SUBJECT: it is the value for NO TREE, and having no subject is one way of having no tree. THE SECOND MEMBER IS NOT HYPOTHETICAL: an envelope-change flight is a work kind that already ships, whose subject is a document, and which had no honest declaration available to it - `[]` would have said it is about nothing and `[repository]` would have said it is about a tree. THE RULE IS RE-EXPRESSED RATHER THAN EXTENDED: a path glob is legal exactly when the kind accepts a subject that HAS a tree, so a fourth subject kind costs one classification rather than an edit to a refusal - which is the difference between a vocabulary and a list of special cases. ANY rather than ALL, and the case emptiness could not have answered at all: a kind accepting both a repository and an envelope takes a glob, because something in the set has paths to select from and refusing would leave that half unbounded. HasTree is TOTAL AND THROWS rather than defaulting, for the reason every totality guard in this package exists: a kind nobody classified would fall through to whichever answer the default happened to be, and that answer silently decides what scope its work kinds may write - permissively in one direction and impossibly in the other. A value joining a closed vocabulary is a version by construction: the only safe response to an unknown value is to halt, so every prior reader is broken by design and told so by this number."
  },
  {
    "version": "0.81.0",
    "surface": "295e1dca7dd0b3abd1392cc2248170fbbed15a352afa90af2207c370c363dc87",
    "note": "A RULE THAT COULD NEVER APPLY SAYS SO, AS A MEMBER RATHER THAN A FOURTH VALUE. ADR-0020 section 2 marks an obligation reading a fact family the work kind cannot produce as inapplicable, decided at authoring and never evaluated - and a decision that leaves no trace is indistinguishable from an omission, which is the failure this vault keeps meeting one surface over. So the answer is STATED: ObligationAttribution.Inapplicable carries the fact family, and gg why renders a `never:` line before the condition. THIS CORRECTS MY OWN DECISION AT SLICE OPEN, made from a slice note rather than the ADR's text. I had recorded that structural inapplicability would take its own fourth plan state, on the grounds that it must not share a word with an evaluated-false does-not-apply. Both sources disagree with me and agree with each other: the ADR says the answer IS does-not-apply, and the note says the slice adds a fourth REASON for a thing to be in that bucket, requiring only that it not share a word with cannot-tell-yet. THE COST SETTLES IT EITHER WAY: a member may be added freely and a value may not, because the only safe response to an unknown value is to halt - so a fourth Attachments value would break every prior reader by design, for a distinction the ADR did not ask for. The attachment stays not-attached, which is true, and the structural reason travels beside it as something no older reader has to understand. IT IS THE FAMILY RATHER THAN A SENTENCE, because Because already carries the words and a caller has to tell `this was measured and did not fire` from `this could never fire` by a null check: the first may fire tomorrow and the second cannot. The value is validated against FactKinds.All, so a reason nobody can check is refused where it is written. AND VALIDATE GAINED ONE ARM, which is the half worth reading twice: a not-attached obligation was required to name a condition, because without one there is no way to tell a false condition from an obligation nobody wrote. Structural inapplicability is precisely the case that HAS no condition - no-file-outside-scope on a subjectless kind applies always, to work that can never answer it - so that rule would have refused the exact answer section 2 exists to give. Inapplicable is what makes it distinguishable now, which is what the rule was protecting."
  },
  {
    "version": "0.82.0",
    "surface": "56b95a1d29a8b369ee0946fe30c47bb908075c38d9b0195b40a88cc5f9555116",
    "note": "CHECK-RUN: THE FIRST DESTINATION WHOSE ACT LEAVES THIS SYSTEM, and ADR-0020 section 4's `comment` corrected on evidence. Section 4 names comment, work-item and message and puts all three on ports it says Platform Architecture already designs. NONE OF THEM HAS MACHINERY: there is no notification port in any form - every match for it in the control plane is Postgres LISTEN/NOTIFY plumbing - and Intent is a URI stored on a lease and rendered into a prompt that nothing resolves in either direction. What DOES have machinery is the check run: a command, a receptor that returns the event, an adapter, and the one write MetadataOnlyTests permits the client to make. AND THE PERMISSION DECIDES IT. The installation holds `checks` and no write on issues or pull requests, asserted live against the forge, so `comment` costs a re-grant every installation must approve and nobody can un-ask - one slice after fifteen declined that same cost for contents:read. This is also strictly better than the slice's own pre-committed cut, where the act would have been admitted, recorded, and never performed. THREE DESTINATION KINDS ALREADY SHIP AND TWO OF THEM ARE PERFORMED BY ADMISSION ITSELF - envelope-change and airspace-registration both land inside the tenant's own stream and registries - so section 3's `the first outward act performed by admission rather than held by the agent` is right only for acts that LEAVE. pull-request leaves and is performed by the RUNNER on the customer's credential, which section 3 says in as many words. This one leaves AND is performed by the control plane, which is what makes section 3's containment argument true rather than nearly true: the agent that drafted the text never holds a credential that could post it. IT IS NOT A COMMENT WEARING A DIFFERENT NAME. A check run does not thread and carries no approve/reject verdict on a forge object; what it carries is a governed, attributed, durable statement on the commit under review, posted only after admission. When a re-grant is being asked for anyway, comment joins on its own merits. preserve-unadmitted is refused on it by a rule that already generalises to every kind that is not pull-request, so section 5 costs a test rather than a check - and the other half is asserted too, so the refusal reads as being about this kind rather than about the key having been retired."
  },
  {
    "version": "0.83.0",
    "surface": "cebcbf322d8e10c6fb9b77bd0637bfc96a09523844ea7fb4145900e4b359b02d",
    "note": "PRODUCES: THE RELATION ADR-0020 SECTION 2 LOST, AND THE COMPARISON THAT WAS NEVER MADE. ADR-0014's relations table has two rows - work kind to subject kinds accepted, and work kind to fact families produced - and ADR-0020 section 2 derived the second from the first. That cannot separate two kinds that share a subject: `implement` and `review` are both about a repository and do not have the same fact vocabulary, so every family reachable from a repository was judged producible by both. ADR-0021 section 3 restores the lost relation and this version carries it. A work kind declares `produces:`, absent is refused rather than defaulted for the same reason `accepts:` is - the default that suggests itself, everything the subject allows, IS the behaviour being corrected, so it would leave every kind whose author never heard of the field exactly as it was. It composes work-kind-only and EnvelopeNarrowing has no member for it, which is the whole reason it is declared rather than derived from `moves:`: moves compose across layers, so a narrowing living in a customer's own repository could delete every scope gate on its own flights by narrowing moves to [read]. AND THE DEFECT FOUND WHILE ADDING IT: EnvelopeDirection.Widening never compared Accepts. It carried a [Composes] operator from the day it shipped, so it passed the drift guard in the static constructor, and was simply absent from the comparison - a work kind that narrowed `accepts:` computed tighter-or-equal and walked through the gate a floor exists to hold. Both fields now compare by containment: declaring a field that was absent is not a widening, reducing it or withdrawing it is."
  },
  {
    "version": "0.84.0",
    "surface": "cebcbf322d8e10c6fb9b77bd0637bfc96a09523844ea7fb4145900e4b359b02d",
    "note": "THE SURFACE IS UNCHANGED FROM 0.83.0 AND THAT IS THE POINT WORTH RECORDING: what moves is a REFUSAL, not a shape. A work kind may no longer declare `produces:` naming a family measured from a tree while its `accepts:` names no subject that has one - work with no tree claiming to measure one. ADR-0021 section 3's veto, refused where an author can still act rather than discovered by a flight that halts. Built as the same refusal section 1 already ships for `accepts:` against `scope: none`: two fields on one document that have to agree, checked with no role in front of them, naming BOTH halves so a reader knows which of two lines to change. TREES RATHER THAN EMPTINESS - `envelope` is a real subject kind with no tree, so `accepts: [envelope]` is as unable to yield a change manifest as `accepts: []` is. FactCategories moves into this package to make that possible: whether a family needs a tree is a property of the vocabulary, and the vocabulary is declared here, so leaving the classification control-plane-side meant either a second copy of it - two computations of one question, where being wrong removes a gate - or a refusal an author could not get until they applied. It joins the FACT fingerprint rather than this one, so fact-vocabulary moves to 0.17.0: adding a family and classifying it become a single reviewable act. NULL IS SILENCE AND NOT EMPTINESS, which a shipped test caught in the first draft of this arm: a document that never declared `accepts:` has not said it takes no subject, and is owed the role refusal rather than a contradiction it did not state."
  },
  {
    "version": "0.85.0",
    "surface": "cebcbf322d8e10c6fb9b77bd0637bfc96a09523844ea7fb4145900e4b359b02d",
    "note": "ONE FACT, ONE SENTENCE. The surface is unchanged again - what moves is where a sentence is composed. The plan rendered 'this work kind cannot produce X, so this rule can never apply to it' and `gg why` rendered 'this kind of work cannot produce X, so this rule can never apply to it': two paraphrases of one claim, composed independently in two repositories, agreeing today because somebody typed carefully twice. A person reading a plan and a person reading `gg why` are checking the same claim and the only thing they can compare is the words, so two spellings is the manifest hazard wearing a different coat - two computations that agree until the first fix to either. Inapplicability.Because is the one composition, it lives beside the vocabulary it names because both sides already reference here, and it throws on a family the contract does not declare rather than composing a reason nobody can look up. Neither repository is allowed a second copy, asserted by a scan over the sentence's own shape rather than over its words - `cannot produce` alone matches the contradiction refusal and three doc comments, and a control that reports the vocabulary instead of the duplication is one that gets exempted into uselessness."
  },
  {
    "version": "0.86.0",
    "surface": "eb7c46e11c94348d79afc5977b982421db0a22e4485aa0cd75ba1beba26d7ecc",
    "note": "A third intent kind, and it is the one FlightIntentKinds predicted in writing: \"adding a third - an issue, a pull request, a ticket - is a contract change, which is what makes it visible; the FIELD to carry it already exists, which is what makes it cheap.\" The prediction was half right. The change is visible and the fields did NOT already exist - a work item is a provider and an id, and neither is a URI or a sentence, so `ticket` arrives with two new members beside it. A VALUE in a closed vocabulary rather than only a member, which is why this number moves: the only safe response to an unknown value is to halt, so an added value breaks every prior reader by design, and that is the point of making it visible. THE ID IS DECLARED AND NEVER PARSED. An id that is a URI, or that carries a path, is refused naming the field - because deriving structure from a string somebody typed is the move slice nine retired when it stopped lifting a repository's provider out of an intent URI's host, and it breaks the same way on a vanity host and on a work item moved between projects. The larger version of that is still live and out of scope here: FlightRepos.From reads only a URI's AbsolutePath and the registry matches on that path alone, so a link at any host resolves to whichever registered entry shares its path. Building a second thing shaped like it would have been the cheapest possible mistake. THE PROVIDER IS A FREE STRING and stays one - gg is public and names no forge, and a closed provider vocabulary here is that rule broken one noun over. Which providers actually resolve is the control plane's knowledge and the Intent port's problem, and the port is not in this slice: nothing fetches a work item, nothing reads its text, and the two members are a declaration rather than an address. A ticket is ONE PAYLOAD CARRIED IN TWO FIELDS, which is the only structurally new thing in the validator: the payload count stops being a count of non-empty strings, so a half-filled ticket is diagnosed as a missing FIELD rather than as an intent carrying nothing - the wrong sentence, and one that sends somebody looking in the wrong half of their document. AND THE CONTAINMENT SHIPS IN THE SAME VERSION, because a kind that names a ticket without it would be one keystroke from a gate that reads the ticket. An obligation whose `rule` decides on a work item's text, or whose `when` attaches on it, is refused WHERE IT IS WRITTEN - named specifically on the `obligations.` precedent, and for the same reason: somebody will type it, because reading the ticket is the obvious thing to want, and the generic unknown-rule sentence would read as a version that has not got round to it yet. The refusal deliberately does NOT offer the list of known rules, because offering it invites a search for the one that reads the ticket. A ticket's title, description and comments are a person's typed words: they are `stated` and can never be `measured`, and a gate resting on them is a gate anyone who can comment on the ticket can answer - which is not who the envelope named. It matches on the citation `intent.` and never on the substring, so `intentional-drift` stays an ordinary typo. EnvelopeNarrowing shares the validator, so the smaller door is closed by the same line."
  },
  {
    "version": "0.87.0",
    "surface": "398b627b805c1fd045041141da2d48c6e18c4fb1d896136638a3fae9726579bb",
    "note": "A strategy can declare how many members should be WARM before anything asks. Everything in a strategy was a maximum - inventory.size is how many the pool may hold, bounds.pool-max is how many may be warm at once - and nothing said how many it SHOULD hold, so warming could only ever happen behind a flight that was already waiting. That is the reactive half; slice twelve deferred the other one in writing as its own pre-committed cut (\"warming ahead of demand to a time-to-warm target\"), and this is the field that makes it expressible. inventory.warm is the first number in a strategy that is not a ceiling, which is to say the only one a decider can be SHORT of. It is a MEMBER and it defaults to ZERO, which is the whole reason this is cheap: a member may be added freely and a value may not, and zero means warm-only-behind-demand, so every strategy already in force reads back and decides exactly as it did. A default of anything else would have turned every applied strategy into a proactive one on the deploy that shipped this. Two refusals, and deliberately not three: warm below zero is refused (below zero is not a smaller kind of zero, it is a number nothing can act on), and warm above bounds.pool-max is refused naming BOTH numbers per Article XI. A warm above inventory.size looks like it deserves its own arm and is UNREACHABLE - pool-max > size is already refused, so anything above the inventory is above the bound first - and shipping an unreachable refusal inside a slice about controls that are never invoked would have been a poor joke. StrategyContainmentTests still holds: int is an allowed shape and 'warm' names no host, endpoint or secret."
  },
  {
    "version": "0.88.0",
    "surface": "b386d6b9330b54dd0837c68db9f262cf01e0cbf6bc6a2ea31ff8bc4b03f43a8b",
    "note": "A pool member can be given a runner identity of its own. Two routes: a resident runner mints a single-use nonce for one named member (POST /v1/pools/{pool}/members/{member}/credential, runner audience, and 403 is the arm that matters - a member presenting its own token must not mint another), and the member redeems that nonce for a real credential (POST /v1/pools/members/redeem, ANONYMOUS by necessity, because a member has no credential yet; 409 is the second attempt being told it is spent).\n\nWhy it exists: nothing has ever run inside a pool member, because nothing could give one an identity. The only mechanism that worked baked a copied developer session.json into an image, which lasts twelve hours and carries the whole developer surface - not something a tenant can operate.\n\nA nonce rather than the credential because GET /containers/gg-pool-*/json is reachable through the scope proxy: anything in a member's environment is readable by an inspect for the life of the container, so what goes in must be worthless once spent.\n\nMemberCredentialIssued carries a runner identity and nothing wider - no session, no principal - because a member runs a customer's code."
  },
  {
    "version": "0.89.0",
    "surface": "c6dc6590d543c1dc10e610108568f1d1b89602afdded3dde9461631337b9b87f",
    "note": "A registered repository can declare the ref work starts from, which is the member that makes a flight about a WORK ITEM able to have a repository at all. A ticket intent is a provider and an id: it names no repository and therefore no ref, so such a flight resolved to nothing, its runner materialized an empty tree, and the agent reported - correctly - that there was nothing there to work on. FlightLaunchRequest.Repository has been declared since flights had selections and says WHICH repository a flight is about; what it could never say is at which ref, and FlightRepo.PinnedRef is required. THE REF CANNOT COME FROM THE FLIGHT and it cannot be guessed. Defaulting to refs/heads/main control-plane-side is the exact substitution FlightRepo refuses in writing - 'a uri naming no ref produces no repository rather than a guessed default branch' - and it is wrong on every repository whose trunk is called something else. A person opening a flight about a work item knows the work item, not the branch policy, so pinning from there would be pinning from the least informed place in the system. It belongs on the registration, beside provider, forge id and credential mode: WHO a repository is, which the registrar knows and nobody downstream does. A MEMBER AND OPTIONAL, so every repository registered to date reads back and resolves exactly as it did - absence is not a default branch, it is a repository a ticket flight cannot start work on, and that flight is refused rather than given a branch somebody invented. Nullable on the way OUT as well, unlike credential and like narrowings: an absent credential and a declared 'required' are the same fact so that one resolves, while these two are different facts and the absence has to survive the round trip. FULLY QUALIFIED, REFUSED OTHERWISE. RepositoryRefs.Validate takes shape and deliberately not existence - whether a ref resolves is the forge's answer and a runner's question, and this repository names no forge and reaches none. A bare 'main' is refused where it is written, because a branch and a tag of one name are two different commits and choosing the namespace on the author's behalf is the guess this member exists to remove. Branches, tags and pull refs all pass: a flight already pins refs/pull/n/head on the BASE repository, which is what makes a fork need no credential of its own, and a validator that knew only about branches would break forks."
  }
]
